# E3 — MVP: Núcleo Funcional com Primeiras Telas

> **Disciplina:** Teoria dos Grafos  
> **Prazo:** 10 de maio de 2026  
> **Peso:** 25% da nota final  

---

## Identificação do Grupo

| Campo | Preenchimento |
|-------|---------------|
| Nome do projeto | GameShop |
| Repositório GitHub | https://github.com/Masana000/GameShop |
| Integrante 1 | Caio Siqueira — 38092361 |
| Integrante 2 | Fillipy Mendes — 37115928 |
| Integrante 3 | Leonardo Masanao — 40731677 |

---

## 1. Como Executar o MVP

**Pré-requisitos:**

```bash
Node.js 18+
```

**Instalação:**

```bash
git clone https://github.com/Masana000/GameShop.git
cd GameShop
npm install
```

**Execução:**

```bash
npm run dev
```

Acesse `http://localhost:5173`

**Saída esperada:**

Tela de seleção de perfil com 8 usuários disponíveis. Ao selecionar um perfil, o sistema redireciona para a loja com recomendações personalizadas geradas pelos algoritmos de grafos.

---

## 2. Algoritmos Implementados

### Algoritmo Principal — BFS (Busca em Largura)

| Campo | Resposta |
|-------|----------|
| Nome do algoritmo | Busca em Largura (BFS) |
| Arquivo de implementação | src/algorithms/bfs.ts |
| Complexidade de tempo | O(V + E) |
| Complexidade de espaço | O(V) |

**Funcionamento:** A partir do usuário selecionado, o BFS percorre o grafo de amizades até profundidade 2, coletando usuários que são amigos dos amigos mas sem conexão direta. Os candidatos são rankeados pela quantidade de amigos em comum.

```typescript
export function suggestFriends(graph: Graph, userId: string): FriendSuggestion[] {
  const directFriends = new Set(graph.neighbors(userId))
  const suggestions = new Map<string, string[]>()

  for (const friend of directFriends) {
    for (const candidate of graph.neighbors(friend)) {
      if (candidate === userId || directFriends.has(candidate)) continue
      if (!suggestions.has(candidate)) suggestions.set(candidate, [])
      suggestions.get(candidate)!.push(friend)
    }
  }

  return Array.from(suggestions.entries())
    .map(([id, mutualFriends]) => ({ userId: id, mutualFriends }))
    .sort((a, b) => b.mutualFriends.length - a.mutualFriends.length)
}
```

### Algoritmo Adicional — Similaridade de Jaccard

| Campo | Resposta |
|-------|----------|
| Nome do algoritmo | Filtragem Colaborativa (Jaccard) |
| Arquivo de implementação | src/algorithms/jaccard.ts |
| Complexidade de tempo | O(N · g), onde N = usuários, g = tamanho médio da biblioteca |
| Complexidade de espaço | O(N) |

**Fórmula:**

```
sim(u, v) = |Biblioteca(u) ∩ Biblioteca(v)| / |Biblioteca(u) ∪ Biblioteca(v)|
```

```typescript
export function jaccard(a: Set<string>, b: Set<string>): number {
  if (a.size === 0 && b.size === 0) return 0
  const intersection = [...a].filter(x => b.has(x)).length
  const union = new Set([...a, ...b]).size
  return intersection / union
}
```

---

## 3. Estrutura do Repositório

```
GameShop/
├── src/
│   ├── core/
│   │   └── Graph.ts
│   ├── algorithms/
│   │   ├── bfs.ts
│   │   └── jaccard.ts
│   ├── io/
│   │   └── reader.ts
│   ├── pages/
│   │   ├── Login.tsx
│   │   ├── Store.tsx
│   │   ├── GameDetail.tsx
│   │   ├── Profile.tsx
│   │   └── Admin.tsx
│   ├── components/
│   │   ├── GameCard.tsx
│   │   └── Layout.tsx
│   ├── state.ts
│   ├── App.tsx
│   └── main.tsx
├── tests/
│   ├── graph.test.ts
│   ├── bfs.test.ts
│   └── jaccard.test.ts
├── data/
│   └── seed.json
├── public/
│   └── covers/
├── docs/
└── README.md
```

**Desvios em relação ao E2:** A arquitetura foi mantida conforme planejado. A persistência utiliza localStorage ao invés de banco de dados, adequado ao escopo acadêmico. A filtragem colaborativa foi formalizada com Jaccard, corrigindo a ambiguidade apontada no E2.

---

## 4. Telas do MVP

### Tela de Entrada — Seleção de Perfil

Tela inicial com grade de perfis disponíveis. O usuário seleciona um perfil e é redirecionado para a loja.

### Tela de Resultado — Loja com Recomendações

Exibe a seção "Recomendado para você" com jogos sugeridos via BFS (amigos que jogam) e Jaccard (perfil similar). Cada card exibe a razão da recomendação. Abaixo, o catálogo completo.

### Tela de Perfil

Exibe biblioteca, amigos e sugestões de amizade geradas pelo BFS com indicação de amigos em comum.

### Tela de Detalhe do Jogo

Capa, descrição, preço e quem na rede do usuário já possui o jogo.

### Painel Admin

Cadastro de novos jogos com upload de capa, lista de jogos cadastrados e gerador de dados aleatórios.

---

## 5. Testes Unitários

| Algoritmo | Caso de teste | Status | Comando |
|-----------|--------------|--------|---------|
| Graph | Caso base: conecta vértices e consulta vizinhos | ✅ | `npm test` |
| Graph | Grafo vazio: nenhum vértice ou vizinho | ✅ | `npm test` |
| Graph | Grafo completo: todos conectados entre si | ✅ | `npm test` |
| BFS | Caso base: sugere amigo de amigo com mútuo correto | ✅ | `npm test` |
| BFS | Grafo vazio: nenhuma sugestão para usuário sem amigos | ✅ | `npm test` |
| BFS | Grafo completo: nenhuma sugestão quando todos já são amigos | ✅ | `npm test` |
| Jaccard | Caso base: calcula similaridade corretamente | ✅ | `npm test` |
| Jaccard | Grafo vazio: similaridade zero para conjuntos vazios | ✅ | `npm test` |
| Jaccard | Grafo completo: recomenda jogo do usuário mais similar | ✅ | `npm test` |

**Como rodar todos os testes:**

```bash
npm test
```

**Resultado:**

```
✓ tests/graph.test.ts    (3 tests)
✓ tests/bfs.test.ts      (3 tests)
✓ tests/jaccard.test.ts  (3 tests)

Tests  9 passed (9)
```

---

## 6. Histórico de Commits

| Hash | Mensagem | Autor |
|------|----------|-------|
| `ac102bc` | feat: graph e algoritmos prontos | Fillipy |
| `5f2a634` | feat: tela de login e loja com recomendacoes | Fillipy |
| `d0235c0` | chore: seed atualizado e docs organizados | Fillipy |
| `ab5d919` | feat: detalhe do jogo, compra e perfil com amigos | Fillipy |
| `038bd05` | feat: admin funcionando, capas adicionadas e moeda em R$ | Fillipy |

---

## 7. O que está funcionando / O que ainda falta

| Funcionalidade | Status | Observação |
|---------------|--------|------------|
| Classe do grafo | ✅ Completo | Lista de adjacência, dirigido e não-dirigido |
| BFS — sugestão de amigos | ✅ Completo | Profundidade 2, rankeado por amigos em comum |
| Jaccard — recomendação de jogos | ✅ Completo | Similaridade entre bibliotecas dos usuários |
| Leitura de arquivo | ✅ Completo | data/seed.json carregado na inicialização |
| Tela de entrada | ✅ Completo | Seleção de perfil |
| Tela de resultado | ✅ Completo | Loja com razão de cada recomendação |
| Tela de perfil e biblioteca | ✅ Completo | |
| Painel admin | ✅ Completo | Cadastro de jogos com upload de capa |
| Testes unitários | ✅ Completo | 9 testes passando |
| Geração aleatória de dados | ✅ Completo | Disponível no painel admin |

---

## Checklist de Entrega

- [x] Repositório público e acessível
- [x] .gitignore configurado
- [x] README com instruções de execução do MVP
- [x] Algoritmo principal executando sem erros
- [x] Tela de entrada e tela de resultado demonstráveis
- [x] 3 testes unitários por algoritmo (todos passando)
- [x] 5+ commits com prefixos semânticos (feat:, fix:, test:, docs:)
- [x] Ao menos 1 arquivo de grafo de exemplo em `data/`

---

*Teoria dos Grafos — Profa. Dra. Andréa Ono Sakai*
